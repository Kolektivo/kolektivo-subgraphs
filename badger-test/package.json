{
  "name": "badger-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ fabianschu/badger-test",
    "create-local": "graph create --node http://localhost:8020/ fabianschu/badger-test",
    "remove-local": "graph remove --node http://localhost:8020/ fabianschu/badger-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fabianschu/badger-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  },
  "devDependencies": {
    "@openzeppelin/subgraphs": "^0.1.7-1"
  }
}
